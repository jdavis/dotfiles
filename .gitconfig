[advice]
    statusHints = false
[color]
    ui = true
[core]
    editor = /usr/bin/vim
    excludesfile = ~/.gitignore
[github]
    user = jdavis
[help]
    autocorrect = 1
[init]
    templatedir = ~/.git_template
[user]
    name = Josh Davis
    email = josh@joshldavis.com
[alias]
    # Shortcuts
    a = add
    b = branch
    bl = blame
    c = commit -v
    cl = clone
    co = checkout
    d = diff
    dc = diff --cached
    f = fetch
    h = help
    l = log
    ls = ls-files
    m = merge
    p = pull
    pu = push -u origin master
    r = remote
    rb = rebase
    rf = reflog
    rs = reset
    rv = revert
    s = status
    sh = show
    sl = shortlog
    sm = submodule
    st = stash
    su = status -uno
    un = reset HEAD

    # New Commands
    amend = commit --amend
    count = shortlog -sn
    ctags = !.git/hooks/ctags
    forward = checkout HEAD^
    last = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit HEAD~10..HEAD~
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    man = help
    unstage = reset HEAD
    up = --set-upstream
    zip = !git ls-files | zip -@ "${PWD##*/}"-`date +%Y%m%d%H%M%S`

    # Found here: http://blog.pagebakers.nl/2009/01/29/git-ignoring-changes-in-tracked-files/
    # Thanks to http://github.com/zdwolfe for tweeting it.
    untrack = update-index --assume-unchanged
    track = update-index --no-assume-unchanged
[push]
	default = simple
